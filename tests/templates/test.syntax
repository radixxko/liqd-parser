:alphanumeric
	/[a-zA-Z_][a-zA-Z_0-9]*/

@variable
@iterator
@definition
@list
@condition
@tagname
@name
@value
  :alphanumeric

@if
  if ( @condition ) { @block }

@for
  for ( @iterator of @list ) { @block }

@attribute
	@name = " @value "

@tag
	< $tag:alphanumeric ...@attribute > < / $tag >

:command
  @if
  @for
  @definition
	@tag

@block
  ...:command

@main
  @block
